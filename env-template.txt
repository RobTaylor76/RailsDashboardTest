# Environment Variables Template
# Copy this content to .env.development and .env.test files

# Database
DATABASE_URL=postgres://postgres:postgres@localhost:5432/dashboard_development

# Redis Configuration
REDIS_URL=redis://localhost:6379

# SSE Configuration
SSE_HOST=localhost
SSE_PORT=3001
SSE_ENDPOINT=/dashboard/stream
SSE_SERVER_TYPE=go
SSE_AUTO_REFRESH=true
SSE_CONNECTION_TIMEOUT=5000
SSE_RETRY_INTERVAL=5000

# Rails Configuration
RAILS_LOG_LEVEL=info
RAILS_MAX_THREADS=25
PORT=3000
PIDFILE=tmp/pids/server.pid

# WebSocket Configuration (Action Cable)
# Set WEBSOCKET_PORT if you have a separate WebSocket server running on a different port
# If not set, Action Cable will use the same port as Rails server (PORT)
# Example: WEBSOCKET_PORT=3001 (for a separate WebSocket server on port 3001)
# WEBSOCKET_PORT=3001
# WEBSOCKET_HOST=localhost
# Ping interval in seconds (Action Cable default is 3 seconds)
# WEBSOCKET_PING_INTERVAL=3
# For production with a separate WebSocket server, you can set:
# WEBSOCKET_URL=wss://your-domain.com/cable

# Logging
LOGRAGE_ENABLED=false

# For .env.test, change the DATABASE_URL to:
# DATABASE_URL=postgres://postgres:postgres@localhost:5432/dashboard_test
